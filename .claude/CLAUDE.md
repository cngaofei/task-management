# Sandbox Environment

This directory is a sandbox copy for safe AI editing.
The original project (with the git repository) is at: `D:\temp\temp2\task-management`

**IMPORTANT:** This is a sandbox — do NOT run git add, git commit, or git push. Git write operations are handled automatically after you finish editing.

For read-only git operations (git status, git log, git diff, git blame, etc.), run them inside the original directory:
```
cd "D:\temp\temp2\task-management" && git <command>
```
Do NOT run git commands in the current sandbox directory — it has no .git folder.
